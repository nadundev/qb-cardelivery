Config = {}

Config.StartLocation = vector4(246.18, -677.92, 37.74, 269.05) -- Location where players start the mission
Config.VehicleSpawnLocations = {                               -- Possible locations where the vehicle can spawn
    vector4(232.13, -606.62, 42.02, 334.48),
}
Config.DeliveryLocation = vector4(287.9, -614.09, 43.42, 180.0) -- Location where the vehicle needs to be delivered -- Increased size of the delivery zone (width, length)                 -- Size of the delivery zone (width, length)
Config.VehicleModel = 'adder'                                   -- Model name of the vehicle to be delivered
Config.DeliveryReward = 10000
